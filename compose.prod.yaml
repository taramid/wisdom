services:
  web:
    image: ghcr.io/your-username/my-symfony-app:latest
    restart: unless-stopped
    ports:
      - "8080:80"
    env_file:
      - .env
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
