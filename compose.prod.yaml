services:
  app:
    image: ghcr.io/taramid/wisdom:latest
    container_name: wisdom-app-1
    restart: unless-stopped

    ports:
      - "8000:80"

    env_file:
      - .env

    environment:
      - APP_ENV=prod
