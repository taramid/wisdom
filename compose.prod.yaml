services:
  app:
    image: ghcr.io/user/project:latest
    container_name: app-1
    restart: unless-stopped

    ports:
      - "8000:80"

    env_file:
      - .env

    environment:
      - APP_ENV=prod
